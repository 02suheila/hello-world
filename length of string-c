#include <stdio.h>
#include<string.h>
int main()
{
    char s1[20];
    int len=0,i=0;
    printf("enter s1 string");
    gets(s1);
    while(s1[i]!= '\0')
    {
        len++;
        i++;
    }
    printf("length of s1 is %d",len);
    return 0;
}
