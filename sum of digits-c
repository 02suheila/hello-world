#include <stdio.h>

int main() {
    int temp,sum=0,num;
    printf("enter a number");
    scanf("%d",&num);
    
    while(num!=0)
    {
        temp=num%10;
        num=num/10;
        sum=sum+temp;
    }
    printf("\n sum of digits=%d",sum);
    return 0;
}



